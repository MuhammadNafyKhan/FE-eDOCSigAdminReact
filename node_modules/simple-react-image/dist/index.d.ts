import React, { DetailedHTMLProps, ImgHTMLAttributes } from 'react';

declare enum EState {
    loading = "loading",
    loaded = "loaded",
    error = "error"
}
interface ImageProps extends DetailedHTMLProps<ImgHTMLAttributes<HTMLImageElement>, HTMLImageElement> {
    fallback?: string;
    errorImage?: string;
    onStateChange?(state: EState): void;
}

declare const ReactImage: React.FC<ImageProps>;

export { ReactImage as Image };
